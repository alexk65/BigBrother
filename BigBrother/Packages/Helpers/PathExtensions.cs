using System.IO;

namespace Packages.Helpers
{
    public static class PathHelper
    {
        public static string Join(params string[] paths)
        {
            if (paths[1].Contains(Path.DirectorySeparatorChar))
            {
                
            }
        }
    }
}
